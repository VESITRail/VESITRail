import { Hero } from "@/components/layout/hero";
import { Header } from "@/components/layout/header";

const Home = () => {
  return (
    <main>
      <Header />
      <Hero />
    </main>
  );
};

export default Home;
